{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "CourseSearchTool Input",
  "description": "Input parameters for CourseSearchTool – keyword search query and result count.",
  "type": "object",
  "properties": {
    "query": {
      "type": "string",
      "description": "Free-text keyword search query."
    },
    "top_k": {
      "type": "integer",
      "description": "Number of top courses to return (1–20).",
      "minimum": 1,
      "maximum": 20,
      "default": 5
    }
  },
  "required": ["query"],
  "additionalProperties": false
} 